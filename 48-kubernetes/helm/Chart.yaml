# =========================================================
# Modulo 48 - helm/Chart.yaml
# =========================================================
# Chart Helm minimo. Helm es un "gestor de paquetes" para K8s:
# empaqueta manifiestos parametrizados (templates/) que se
# renderizan sustituyendo valores desde values.yaml.
# =========================================================
apiVersion: v2
name: kubernetes-demo
description: Modulo 48 - Chart Helm minimo para el demo de Kubernetes
type: application
version: 1.0.0
appVersion: "1.0.0"
